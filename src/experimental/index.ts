/**
 * Experimental effect-playwright APIs.
 *
 * These APIs may change without the compatibility guarantees of the main
 * package entrypoint.
 *
 * @since 0.1.0
 * @packageDocumentation
 */

/**
 * Utilities for traversing pages, frames, and frame-navigation events.
 *
 * @since 0.2.0
 */
export * as BrowserUtils from "./browser-utils";
/**
 * Scoped browser provisioning for Effect programs.
 *
 * @since 0.7.0
 */
export * as PlaywrightSpawner from "./playwright-spawner";
